<!DOCTYPE html>
<html lang="en">
<head>
</a> <meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="God’swill | Portfolio">
<meta name="twitter:description" content="Frontend Developer Portfolio">
<meta name="twitter:image" content="https://i.imgur.com/2DhmtJ4.png"><!-- OPEN GRAPH (WhatsApp, Facebook, LinkedIn preview) -->
<meta property="og:title" content="God’swill | Portfolio">
<meta property="og:description" content="Frontend Developer skilled in HTML, CSS, JavaScript. Explore my projects and contact me.">
<meta property="og:type" content="website">
<meta property="og:url" content="https://godswillokorie7.github.io/portfolio/">
<meta property="og:image" content="https://i.imgur.com/2DhmtJ4.png">
  <meta property="og:title" content="God’swill Portfolio">
<meta property="og:description" content="Frontend Developer Portfolio built with HTML, CSS & JavaScript">
<meta property="og:type" content="website"><meta name="description" content="God’swill Portfolio - Frontend Developer skilled in HTML, CSS, JavaScript, and modern web design.">
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>God’swill | Portfolio</title>
<!-- EMAILJS -->
<script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
<script>
  (function(){
    emailjs.init("nzRFouNwLbHZvrGOJ");
  })();
</script>
<style>
*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family: Arial, sans-serif;
}
html{
  scroll-behavior:smooth;
}
body{
  background: radial-gradient(circle at top, #0f172a, #020617);
  color:#e2e8f0;
  min-height:100vh;
  transition:0.4s ease;
}
/* BACKGROUND GLOW */
body::before{
  content:"";
  position:fixed;
  inset:0;
  background:
    radial-gradient(circle at 20% 20%, rgba(56,189,248,0.15), transparent 40%),
    radial-gradient(circle at 80% 30%, rgba(14,165,233,0.12), transparent 40%),
    radial-gradient(circle at 50% 80%, rgba(99,102,241,0.10), transparent 40%);
  z-index:-1;
}
/* NAV */
nav{
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:18px 10%;
  background: rgba(17, 24, 39, 0.6);
  backdrop-filter: blur(12px);
  position:sticky;
  top:0;
  border-bottom:1px solid rgba(56,189,248,0.2);
}
nav h2{
  color:#38bdf8;
}
nav a{
  color:white;
  margin-left:15px;
  text-decoration:none;
  font-weight:bold;
}
nav a:hover{
  color:#38bdf8;
}
/* HERO */
.hero{
  text-align:center;
  padding:80px 20px;
}
.hero img{
  width:140px;
  height:140px;
  border-radius:50%;
  border:3px solid #38bdf8;
  box-shadow:0 0 25px rgba(56,189,248,0.3);
}
.hero h1{
  font-size:42px;
  color:#38bdf8;
  margin-top:15px;
}
.hero p{
  min-height:25px;
  color:#cbd5e1;
}
/* BUTTON */
.btn{
  margin-top:15px;
  padding:12px 22px;
  border:none;
  background: linear-gradient(135deg,#38bdf8,#0ea5e9);
  color:white;
  font-weight:bold;
  border-radius:12px;
  cursor:pointer;
  transition:0.3s;
}

.btn:hover{
  transform: scale(1.05);
  filter: brightness(1.1);
}
/* SECTIONS */
section{
  padding:70px 10%;
  opacity:0;
  transform:translateY(40px);
  transition:0.7s ease;
}
section.show{
  opacity:1;
  transform:translateY(0);
}
h2{
  color:#38bdf8;
  margin-bottom:20px;
}
/* GRID */
.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
  gap:20px;
}
/* GLASS CARDS */
.card, .project{
  background: rgba(30, 41, 59, 0.55);
  backdrop-filter: blur(14px);
  border:1px solid rgba(56,189,248,0.15);
  padding:20px;
  border-radius:14px;
  transition:0.3s;
}
.card:hover, .project:hover{
  transform:translateY(-8px);
}
/* INPUTS */
input, textarea{
  width:60%;
  max-width:400px;
  padding:10px;
  margin:5px;
  border:none;
  border-radius:8px;
}
/* FOOTER */
footer{
  text-align:center;
  padding:40px;
  background: rgba(17, 24, 39, 0.6);
}
/* LIGHT MODE */
.light{
  background:white;
  color:black;
}
.light nav,
.light footer{
  background:#f1f5f9;
}
.light .card,
.light .project{
  background:#f8fafc;
}
/* MOBILE */
@media(max-width:768px){
  nav{
    flex-direction:column;
    gap:10px;
  }
  .hero h1{
    font-size:30px;
  }
  input, textarea{
    width:90%;
  }
.whatsapp-float{
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 60px;
  height: 60px;
  background: #25D366;
  color: white;
  font-size: 28px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 50%;
  text-decoration: none;
  box-shadow: 0 10px 25px rgba(0,0,0,0.3);
  z-index: 999;
  transition: 0.3s ease;
  animation: pulse 1.8s infinite;
}
/* hover effect */
.whatsapp-float:hover{
  transform: scale(1.15);
  box-shadow: 0 0 25px #25D366;
}
/* pulse animation */
@keyframes pulse{
  0%{
    box-shadow: 0 0 0 0 rgba(37,211,102,0.5);
  }
  70%{
    box-shadow: 0 0 0 15px rgba(37,211,102,0);
  }
  100%{
    box-shadow: 0 0 0 0 rgba(37,211,102,0);
  }
}
/* tooltip */
.whatsapp-float::after{
  content: "Chat with me";
  position: absolute;
  right: 75px;
  background: #111827;
  color: white;
  padding: 6px 10px;
  border-radius: 6px;
  font-size: 12px;
  opacity: 0;
  transform: translateX(10px);
  transition: 0.3s;
  white-space: nowrap;
}
.whatsapp-float:hover::after{
  opacity: 1;
  transform: translateX(0);
}
  transform: scale(1.1);
  box-shadow: 0 0 20px #25D366;
}.subtitle{
  margin-top:10px;
  font-size:16px;
  color:#94a3b8;
  max-width:500px;
  margin-left:auto;
  margin-right:auto;
  line-height:1.6;
}
.hero-buttons{
  margin-top:20px;
  display:flex;
  gap:10px;
  justify-content:center;
  flex-wrap:wrap;
}.project-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit, minmax(250px, 1fr));
  gap:20px;
  margin-top:20px;
}
.project-card{
  background: rgba(30, 41, 59, 0.6);
  backdrop-filter: blur(12px);
  padding:20px;
  border-radius:15px;
  border:1px solid rgba(56,189,248,0.2);
  transition:0.3s ease;
}
.project-card:hover{
  transform: translateY(-10px);
  box-shadow: 0 15px 30px rgba(0,0,0,0.3);
  border-color:#38bdf8;
}
.project-card h3{
  color:#38bdf8;
  margin-bottom:10px;
}
.project-card p{
  font-size:14px;
  color:#cbd5e1;
  margin-bottom:15px;
}
.project-card a{
  color:#0ea5e9;
  text-decoration:none;
  font-weight:bold;
}
.project-card a:hover{
  text-decoration:underline;
}#progress-bar{
  position:fixed;
  top:0;
  left:0;
  height:4px;
  width:0%;
  background:linear-gradient(90deg,#38bdf8,#0ea5e9);
  z-index:9999;
  transition:width 0.1s ease;
}.skill{
  margin-bottom:15px;
}
.bar{
  width:100%;
  height:10px;
  background:#1e293b;
  border-radius:20px;
  overflow:hidden;
}
.fill{
  height:100%;
  width:0;
  border-radius:20px;
  transition:2s ease;
  background:linear-gradient(90deg,#38bdf8,#0ea5e9);
}
.html{ width:90%; }
.css{ width:85%; }
.js{ width:70%; }.about-intro{
  max-width:600px;
  margin:10px auto 30px auto;
  color:#cbd5e1;
  line-height:1.6;
}
.timeline{
  position:relative;
  max-width:700px;
  margin:auto;
  padding-left:20px;
  border-left:2px solid #38bdf8;
}
.timeline-item{
  position:relative;
  margin-bottom:30px;
  padding-left:20px;
}
.timeline-item .dot{
  position:absolute;
  left:-9px;
  top:5px;
  width:16px;
  height:16px;
  background:#38bdf8;
  border-radius:50%;
  box-shadow:0 0 10px #38bdf8;
}
.timeline-item .content{
  background:rgba(30,41,59,0.5);
  padding:15px;
  border-radius:12px;
  backdrop-filter:blur(10px);
  border:1px solid rgba(56,189,248,0.2);
  transition:0.3s;
}
.timeline-item .content:hover{
  transform:translateX(5px);
}
.timeline-item h3{
  color:#38bdf8;
  margin-bottom:5px;
}.modal{
  display:none;
  position:fixed;
  top:0;
  left:0;
  width:100%;
  height:100%;
  background:rgba(0,0,0,0.7);
  backdrop-filter:blur(6px);
  justify-content:center;
  align-items:center;
  z-index:9999;
}
.modal-content{
  background: rgba(15, 23, 42, 0.6);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  padding: 25px;
  border-radius: 16px;
  max-width: 500px;
  width: 90%;
  border: 1px solid rgba(56,189,248,0.3);
  text-align: center;
  animation: pop 0.25s ease;
  box-shadow: 0 0 25px rgba(56,189,248,0.2);
}
.modal-content h2{
  color:#38bdf8;
}
.modal-content p{
  color:#cbd5e1;
  line-height:1.6;
}
@keyframes pop{
  from{transform:scale(0.8); opacity:0;}
  to{transform:scale(1); opacity:1;}
}
.close{
  float:right;
  font-size:26px;
  cursor:pointer;
  color:white;
}.reveal{
  opacity:0;
  transform: translateY(40px);
  transition: all 0.8s ease;
}
.reveal.active{
  opacity:1;
  transform: translateY(0);
}html{
  scroll-behavior:smooth;
}
section{
  padding:80px 20px;
}#scrollTopBtn{
  position:fixed;
  bottom:90px;
  right:20px;
  width:45px;
  height:45px;
  border-radius:50%;
  border:none;
  background:#38bdf8;
  color:white;
  font-size:18px;
  cursor:pointer;
  display:none;
  box-shadow:0 10px 20px rgba(0,0,0,0.3);
  z-index:999;
}#loader{
  position:fixed;
  width:100%;
  height:100%;
  background:#0f172a;
  display:flex;
  justify-content:center;
  align-items:center;
  color:#38bdf8;
  font-size:22px;
  z-index:99999;
  animation: fadeOut 1s ease forwards;
  animation-delay: 1.5s;
}
@keyframes fadeOut{
  to{
    opacity:0;
    visibility:hidden;
  }
}body.light-mode{
  background:white;
  color:black;
}
body.light-mode .project-card{
  background:#f1f5f9;
  color:black;
}
body.light-mode .modal-content{
  background:white;
  color:black;
}.github-card{
  max-width:400px;
  margin:30px auto;
  padding:25px;
  border-radius:20px;
  text-align:center;
  background:rgba(15,23,42,0.6);
  backdrop-filter:blur(15px);
  border:1px solid rgba(56,189,248,0.2);
  box-shadow:0 10px 30px rgba(0,0,0,0.3);
  transition:0.3s ease;
}
.github-card:hover{
  transform:translateY(-8px);
}
.github-card img{
  width:90px;
  height:90px;
  border-radius:50%;
  margin-bottom:15px;
}
.github-card h3{
  color:#38bdf8;
  margin-bottom:10px;
}
.github-card p{
  color:#cbd5e1;
  line-height:1.6;
}.stack-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
  gap:20px;
  margin-top:30px;
}
.stack-card{
  padding:25px;
  border-radius:18px;
  background:rgba(15,23,42,0.6);
  backdrop-filter:blur(15px);
  border:1px solid rgba(56,189,248,0.15);
  transition:0.3s ease;
  box-shadow:0 10px 25px rgba(0,0,0,0.2);
}
.stack-card:hover{
  transform:translateY(-10px) scale(1.02);
  border-color:#38bdf8;
  box-shadow:0 0 25px rgba(56,189,248,0.3);
}
.stack-card h3{
  color:#38bdf8;
  margin-bottom:10px;
}
.stack-card p{
  color:#cbd5e1;
  line-height:1.6;
}#particles{
  position:fixed;
  width:100%;
  height:100%;
  top:0;
  left:0;
  background:
    radial-gradient(circle at 20% 20%, rgba(56,189,248,0.15), transparent 20%),
    radial-gradient(circle at 80% 30%, rgba(14,165,233,0.1), transparent 20%),
    radial-gradient(circle at 50% 80%, rgba(56,189,248,0.08), transparent 20%);
  z-index:-1;
  animation: moveParticles 10s linear infinite alternate;
}
@keyframes moveParticles{
  from{
    transform:translateY(0px);
  }
  to{
    transform:translateY(-20px);
  }
}.cursor-glow{
  width:20px;
  height:20px;
  background:rgba(56,189,248,0.4);
  border-radius:50%;
  position:fixed;
  pointer-events:none;
  transform:translate(-50%,-50%);
  z-index:99999;
  box-shadow:0 0 20px #38bdf8;
}@media(max-width:768px){
  .hero h1{
    font-size:32px;
  }
  .subtitle{
    font-size:14px;
    padding:0 10px;
  }
  .hero-buttons{
    flex-direction:column;
    align-items:center;
  }
  .btn{
    width:90%;
  }
  .project-grid,
  .stack-grid{
    grid-template-columns:1fr;
  }
  .timeline{
    padding-left:15px;
  }
  .modal-content{
    width:95%;
  }
}.btn {
  background: linear-gradient(135deg,#38bdf8,#0ea5e9);
  color: #ffffff;
  font-weight: bold;
  display: inline-block;
  padding: 12px 22px;
  border-radius: 12px;
  text-decoration: none;
</style>
</head>
<body>
  <div id="particles"></div>
  <div id="loader">
  <h2>God’swill Portfolio</h2>
</div>
  <button id="scrollTopBtn" onclick="scrollToTop()">↑</button>
  <div id="progress-bar"></div>
  <a href="https://wa.me/2348071704558?text=Hi%20God’swill%2C%20I%20visited%20your%20portfolio%20and%20want%20to%20reach%20out"
   class="whatsapp-float"
   target="_blank">
   💬
</a>
</a>
<nav>
  <h2>God’swill</h2>
  <div>
    <a href="#about">About</a>
    <a href="#skills">Skills</a>
    <a href="#projects">Projects</a>
    <a href="#todo">To-Do</a>
    <a href="#contact">Contact</a>
  </div>
</nav>
<!-- HERO -->
<div class="hero">
  <img src="https://i.imgur.com/2DhmtJ4.png">
  <h1>Hi, I'm God’swill</h1>
  <p id="typed"></p>
  <p class="subtitle">
    Frontend Developer • Building modern web experiences with HTML, CSS & JavaScript
  </p>
  <div class="hero-buttons">
    <button class="btn" onclick="toggleTheme()">Toggle Theme</button>
    <a href="https://wa.me/2348071704558?text=Hi%20God’swill%2C%20I%20visited%20your%20portfolio%20and%20want%20to%20reach%20out" target="_blank">
      
    </a>
  </div>
</div>
<!-- ABOUT -->
<section id="about" class="reveal">
  <h2>About Me</h2>
  <p class="about-intro">
    I’m God’swill, a frontend developer in training focused on building modern, clean, and responsive web experiences.
  </p>
  <div class="timeline">
    <div class="timeline-item">
      <span class="dot"></span>
      <div class="content">
        <h3>2025 - Started Coding</h3>
        <p>Began learning HTML, CSS, and JavaScript fundamentals.</p>
      </div>
    </div>
    <div class="timeline-item">
      <span class="dot"></span>
      <div class="content">
        <h3>2025 - Built First Projects</h3>
        <p>Created my first portfolio website and simple web apps.</p>
      </div>
    </div>
    <div class="timeline-item">
      <span class="dot"></span>
      <div class="content">
        <h3>2026 - Growing Skills</h3>
        <p>Improving frontend skills and building more advanced projects.</p>
      </div>
    </div>
  </div>
</section>
<!-- SKILLS -->
<section id="skills" class="reveal">
  <h2>Skills</h2>
  <div class="skill">
    <p>HTML</p>
    <div class="bar"><div class="fill html"></div></div>
  </div>
  <div class="skill">
    <p>CSS</p>
    <div class="bar"><div class="fill css"></div></div>
  </div>
  <div class="skill">
    <p>JavaScript</p>
    <div class="bar"><div class="fill js"></div></div>
  </div>
</section>
<!-- PROJECTS -->
<section id="projects" class="reveal">
  <h2>Projects</h2>
  <div class="project-grid">
    <div class="project-card">
      <h3>Portfolio Website</h3>
      <p>My personal developer portfolio built with HTML, CSS & JavaScript.</p>
      <button class="btn" onclick="openModal('portfolioModal')">
  View Project
</button>
    </div>
    <div class="project-card">
      <h3>Future Project</h3>
      <p>More projects coming soon as I improve my development skills.</p>
      <a class="btn" href="calculator.html" target="_blank">View Live</a>
    </div>
  </div>
  <section id="github" class="reveal">
  <h2>GitHub</h2>
  <div class="github-card">
    <img src="https://avatars.githubusercontent.com/u/9919?s=200&v=4">
    <h3>Godswillokorie7</h3>
    <p>
      Frontend developer in training passionate about modern web development and UI design.
    </p>
    <a href="https://github.com/Godswillokorie7" target="_blank">
      <button class="btn">Visit GitHub</button>
    </a>
  </div>
</section>
<section id="stack" class="reveal">
  <h2>Tech Stack</h2>
  <div class="stack-grid">
    <div class="stack-card">
      <h3>HTML5</h3>
      <p>Semantic and responsive page structure.</p>
    </div>
    <div class="stack-card">
      <h3>CSS3</h3>
      <p>Modern styling, animations, and layouts.</p>
    </div>
    <div class="stack-card">
      <h3>JavaScript</h3>
      <p>Interactive frontend functionality and UI behavior.</p>
    </div>
    <div class="stack-card">
      <h3>GitHub</h3>
      <p>Version control and project deployment.</p>
    </div>
  </div>
</section>
<!-- TO DO -->
<section id="todo" class="reveal">
<h2>To-Do App</h2>
<input id="taskInput" placeholder="Enter task">
<button class="btn" onclick="addTask()">Add</button>
<ul id="taskList"></ul>
</section>
<!-- CONTACT -->
<section id="contact" class="reveal">
<a href="https://wa.me/2348071704558" target="_blank">
</a>
<h2>Contact Me</h2>
<input id="name" placeholder="Your Name"><br>
<input id="email" placeholder="Your Email"><br>
<textarea id="message" placeholder="Your Message"></textarea><br>
<button class="btn" onclick="sendMail()">Send Message</button>
<p id="status"></p>
  <button class="btn">Hire Me</button>
  <button class="btn">Message Me on WhatsApp</button>
</section>
<!-- FOOTER -->
<footer>
<p>© 2026 God’swill Portfolio</p>
</footer>
<script>
  window.addEventListener("load", ()=>{
  let savedTheme = localStorage.getItem("theme");
  if(savedTheme === "light"){
    document.body.classList.add("light-mode");
  }
});
function openModal(id){
  document.getElementById(id).style.display = "flex";
}
function closeModal(id){
  document.getElementById(id).style.display = "none";
}
// close when clicking outside modal
window.onclick = function(event){
  let modal = document.getElementById("portfolioModal");
  if(event.target === modal){
    modal.style.display = "none";
  }
}
/* THEME */
function toggleTheme(){
  document.body.classList.toggle("light-mode");

  // save stat
  if(document.body.classList.contains("light-mode")){
    localStorage.setItem("theme", "light");
  } else {
    localStorage.setItem("theme", "dark");
  }
}
/* TYPING */
const text = "Frontend Developer • HTML • CSS • JavaScript";
let i = 0;
function type(){
  const el = document.getElementById("typed");
  if(i < text.length){
    el.innerHTML += text.charAt(i++);
    setTimeout(type, 60);
  }
}
type();

/* SCROLL */
const sections = document.querySelectorAll("section");

function reveal(){
  const trigger = window.innerHeight * 0.85;

  sections.forEach(sec=>{
    if(sec.getBoundingClientRect().top < trigger){
      sec.classList.add("show");
    }
  });
}

window.addEventListener("scroll", reveal);
reveal();

/* TODO */
function addTask(){
  const input = document.getElementById("taskInput");

  if(!input.value) return;

  const li = document.createElement("li");
  li.textContent = input.value;

  const btn = document.createElement("button");
  btn.textContent = "X";
  btn.style.background="red";
  btn.style.color="white";

  btn.onclick = ()=>li.remove();

  li.appendChild(btn);
  document.getElementById("taskList").appendChild(li);

  input.value="";
}

/* GITHUB */
async function loadRepos(){
  const container = document.getElementById("repo-container");
  const res = await fetch("https://api.github.com/users/Godswillokorie7/repos");
  const data = await res.json();
  data.slice(0,6).forEach(repo=>{
    const div = document.createElement("div");
    div.className="project";
    div.innerHTML = `
      <h3>${repo.name}</h3>
      <p>${repo.description || "No description"}</p>
      <a href="${repo.html_url}" target="_blank">View</a>
    `;
    container.appendChild(div);
  });
}
loadRepos();
/* EMAILJS */
function sendMail(){
  const name = document.getElementById("name").value;
  const email = document.getElementById("email").value;
  const message = document.getElementById("message").value;
  const status = document.getElementById("status");

  if(!name || !email || !message){
    status.innerText="Please fill all fields";
    return;
  }

  const params = {
    from_name:name,
    from_email:email,
    message:message
  };
  emailjs.send("service_4l45n55", "template_kbvaekr", params)
    .then(()=>{
      status.innerText="Message sent successfully ✅";
    })
    .catch(()=>{
      status.innerText="Failed to send ❌";
    });
}window.onscroll = function(){
  let winScroll = document.body.scrollTop || document.documentElement.scrollTop;
  let height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
  let scrolled = (winScroll / height) * 100;
  document.getElementById("progress-bar").style.width = scrolled + "%";
};window.addEventListener("load", ()=>{
  document.querySelectorAll(".fill").forEach(el=>{
    el.style.width = el.classList.contains("html") ? "90%" :
                     el.classList.contains("css") ? "85%" : "70%";
  });
});
function revealOnScroll(){
  let elements = document.querySelectorAll(".reveal");

  elements.forEach(el=>{
    let windowHeight = window.innerHeight;
    let elementTop = el.getBoundingClientRect().top;
    let visiblePoint = 100;

    if(elementTop < windowHeight - visiblePoint){
      el.classList.add("active");
    }
  });
}

window.addEventListener("scroll", revealOnScroll);let scrollBtn = document.getElementById("scrollTopBtn");

window.addEventListener("scroll", ()=>{
  if(document.documentElement.scrollTop > 200){
    scrollBtn.style.display = "block";
  } else {
    scrollBtn.style.display = "none";
  }
});

function scrollToTop(){
  window.scrollTo({top:0, behavior:"smooth"});
}const roles = [
  "Frontend Developer",
  "UI Enthusiast",
  "Web Designer",
  "JavaScript Learner"
];

let roleIndex = 0;
let charIndex = 0;
let currentRole = "";
let isDeleting = false;

function typeEffect(){
  const typed = document.getElementById("typed");

  currentRole = roles[roleIndex];

  if(isDeleting){
    typed.textContent = currentRole.substring(0, charIndex--);
  } else {
    typed.textContent = currentRole.substring(0, charIndex++);
  }

  let speed = isDeleting ? 50 : 100;

  if(!isDeleting && charIndex === currentRole.length){
    speed = 1200;
    isDeleting = true;
  }

  if(isDeleting && charIndex === 0){
    isDeleting = false;
    roleIndex = (roleIndex + 1) % roles.length;
  }
  setTimeout(typeEffect, speed);
}
typeEffect();
const cursor = document.querySelector(".cursor-glow");

document.addEventListener("mousemove",(e)=>{
  cursor.style.left = e.clientX + "px";
  cursor.style.top = e.clientY + "px";
});
</script>
<div id="portfolioModal" class="modal">
  <div class="modal-content">
    <span class="close" onclick="closeModal('portfolioModal')">&times;</span>

    <h2>Portfolio Website</h2>

    <p>
      This is my personal developer portfolio built using HTML, CSS, and JavaScript.
      It includes modern UI design, animations, WhatsApp integration, and responsive layout.
    </p>

    <p>
      Built to showcase my frontend development journey and skills.
    </p>

    <a href="https://godswillokorie7.github.io/portfolio/" target="_blank">
      <button class="btn">Open Live Site</button>
    </a>
  </div>
</div>
<div class="cursor-glow"></div>
</body>
</html> 
